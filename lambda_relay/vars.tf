variable "subnet_id" {
    description = "The subnet id that we will be using to setup our resources"
    type = string
}

variable "security_group_id" {
    description = "The security group id that we will be using to setup our resources"
    type = string
}